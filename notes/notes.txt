for me just, thinking better, remove the llms keys via front end

2. "Fix" confirmation — what does the Agent endpoint receive?
    error_report.id

    i believe it can find the payload no?


 Error list — columns visible in the list row (collapsed state)
